# AMEX Resolve AI Backend

Backend scaffold for API, investigation workflow, persistence, and secure AI integrations.

Current endpoint:

- `GET /api/health`

Run locally:

```bash
npm run dev --prefix backend
```
